name: cli
description: LocalSend headless CLI — receive/send files over LAN without GUI.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.5.0

dependencies:
  args: 2.4.2
  basic_utils: 5.7.0
  common:
    path: ../common
  convert: ^3.1.0
  dart_mappable: ^4.2.0
  mime: ^1.0.0
  path: 1.9.0
  uuid: 4.5.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
